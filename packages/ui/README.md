# packages/ui

Presentational React components.

Takes props, emits events. Does not call IPC, does not fetch, does not know
about Electron or the database.

The component that matters most is the page viewer with bounding-box
highlighting — it is how a reviewer checks a value against the document in one
click, and it is the deliverable of milestone 1.

Not implemented yet.
