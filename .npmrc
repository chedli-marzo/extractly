# Every direct dependency runs with main-process privileges on a machine holding
# confidential engineering documents. Install scripts are a supply-chain surface,
# so they are off by default. A native module that genuinely needs a build step
# is allowlisted explicitly in pnpm-workspace.yaml (onlyBuiltDependencies).
ignore-scripts=true
